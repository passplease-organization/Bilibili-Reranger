name: build docker
on:
  workflow_dispatch:
  push:
    branches:
      - backend
    paths:
      - .devcontainer/Dockerfile

jobs:
  call-workflow-passing-data:
    permissions:
      contents: read
      packages: write
    uses: passplease/Bilibili-Reranger/.github/workflows/docker.yml@master
    with:
      ref: "backend"